<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Color x:Key="Color.Bg">#FF000000</Color>
    <Color x:Key="Color.Panel">#FF0D0D0D</Color>
    <Color x:Key="Color.PanelBorder">#FF2A2A2A</Color>
    <Color x:Key="Color.Text">#FFF0F0F0</Color>
    <Color x:Key="Color.Muted">#FF9CA3AF</Color>
    <Color x:Key="Color.Accent">#FFED1C24</Color>
    <Color x:Key="Color.AccentHover">#FFFF3A42</Color>
    <Color x:Key="Color.Active">#FF22C55E</Color>
    <Color x:Key="Color.ActiveBg">#FF163A26</Color>
    <Color x:Key="Color.Warning">#FFFACC15</Color>
    <Color x:Key="Color.WarningBg">#FF2A2208</Color>
    <Color x:Key="Color.Info">#FF3B82F6</Color>
    <Color x:Key="Color.InfoBg">#FF1E3A5F</Color>
    <Color x:Key="Color.Button">#FF1A1A1A</Color>
    <Color x:Key="Color.ButtonHover">#FF2A2A2A</Color>
    <Color x:Key="Color.LogBg">#FF000000</Color>
    <Color x:Key="Color.RowAlt">#FF080808</Color>
    <Color x:Key="Color.Purple">#FF9B59B6</Color>
    <Color x:Key="Color.PurpleBg">#FF2D1A3A</Color>
    <Color x:Key="Color.FailedBg">#FF3A1212</Color>

    <SolidColorBrush x:Key="Brush.Bg" Color="{StaticResource Color.Bg}" />
    <SolidColorBrush x:Key="Brush.Panel" Color="{StaticResource Color.Panel}" />
    <SolidColorBrush x:Key="Brush.PanelBorder" Color="{StaticResource Color.PanelBorder}" />
    <SolidColorBrush x:Key="Brush.Text" Color="{StaticResource Color.Text}" />
    <SolidColorBrush x:Key="Brush.Muted" Color="{StaticResource Color.Muted}" />
    <SolidColorBrush x:Key="Brush.Accent" Color="{StaticResource Color.Accent}" />
    <SolidColorBrush x:Key="Brush.AccentHover" Color="{StaticResource Color.AccentHover}" />
    <SolidColorBrush x:Key="Brush.Active" Color="{StaticResource Color.Active}" />
    <SolidColorBrush x:Key="Brush.ActiveBg" Color="{StaticResource Color.ActiveBg}" />
    <SolidColorBrush x:Key="Brush.Warning" Color="{StaticResource Color.Warning}" />
    <SolidColorBrush x:Key="Brush.WarningBg" Color="{StaticResource Color.WarningBg}" />
    <SolidColorBrush x:Key="Brush.Info" Color="{StaticResource Color.Info}" />
    <SolidColorBrush x:Key="Brush.InfoBg" Color="{StaticResource Color.InfoBg}" />
    <SolidColorBrush x:Key="Brush.Button" Color="{StaticResource Color.Button}" />
    <SolidColorBrush x:Key="Brush.ButtonHover" Color="{StaticResource Color.ButtonHover}" />
    <SolidColorBrush x:Key="Brush.LogBg" Color="{StaticResource Color.LogBg}" />
    <SolidColorBrush x:Key="Brush.RowAlt" Color="{StaticResource Color.RowAlt}" />
    <SolidColorBrush x:Key="Brush.Purple" Color="{StaticResource Color.Purple}" />
    <SolidColorBrush x:Key="Brush.PurpleBg" Color="{StaticResource Color.PurpleBg}" />
    <SolidColorBrush x:Key="Brush.FailedBg" Color="{StaticResource Color.FailedBg}" />

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
    </Style>

    <Style TargetType="Grid">
        <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border" Background="{StaticResource Brush.Panel}"
                            BorderBrush="{StaticResource Brush.PanelBorder}" BorderThickness="1,1,1,0"
                            Margin="0,0,4,0" Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Bg}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="ToolkitComboBoxTemplate" TargetType="ComboBox">
        <Grid>
            <ToggleButton x:Name="ToggleButton"
                          Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          Focusable="False"
                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                          ClickMode="Press">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="20" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                  Margin="8,4"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />
                                <Path Grid.Column="1"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                      Fill="{StaticResource Brush.Muted}" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>
            <ContentPresenter x:Name="ContentSite"
                              Margin="8,4,24,4"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Center"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                              IsHitTestVisible="False" />
            <Popup x:Name="Popup"
                   Placement="Bottom"
                   AllowsTransparency="True"
                   Focusable="False"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   PopupAnimation="Slide">
                <Border x:Name="DropDownBorder"
                        MinWidth="{TemplateBinding ActualWidth}"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                        Background="{StaticResource Brush.Panel}"
                        BorderBrush="{StaticResource Brush.PanelBorder}"
                        BorderThickness="1">
                    <ScrollViewer SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{StaticResource ToolkitComboBoxTemplate}" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="CaretBrush" Value="{StaticResource Brush.Text}" />
    </Style>

    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
    </Style>

    <Style TargetType="ScrollViewer">
        <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
    </Style>

    <Style x:Key="ToolkitDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="RowBackground" Value="{StaticResource Brush.Bg}" />
        <Setter Property="AlternatingRowBackground" Value="{StaticResource Brush.RowAlt}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="CanUserResizeColumns" Value="True" />
    </Style>

    <Style x:Key="DataGridCellText" TargetType="TextBlock">
        <Setter Property="TextTrimming" Value="None" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0,0,1,1"
                            Padding="{TemplateBinding Padding}">
                        <TextBlock Text="{TemplateBinding Content}"
                                   TextTrimming="None"
                                   TextWrapping="NoWrap"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Foreground="{TemplateBinding Foreground}"
                                   FontFamily="{TemplateBinding FontFamily}"
                                   FontSize="{TemplateBinding FontSize}"
                                   FontWeight="{TemplateBinding FontWeight}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="Padding" Value="6,4" />
    </Style>

    <Style x:Key="DataGridWrapCell" TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="TestResultWinnerCellCpu" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding CpuFailed}" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.FailedBg}" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding CpuFailed}" Value="False" />
                    <Condition Binding="{Binding BestMode}" Value="CPU" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="TestResultWinnerCellApu" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding ApuFailed}" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.FailedBg}" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ApuFailed}" Value="False" />
                    <Condition Binding="{Binding BestMode}" Value="APU" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="TestResultWinnerCellGpu" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding GpuFailed}" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.FailedBg}" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding GpuFailed}" Value="False" />
                    <Condition Binding="{Binding BestMode}" Value="GPU" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>
    <Style x:Key="TestResultWinnerCellHybrid" TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding HybridFailed}" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.FailedBg}" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding HybridFailed}" Value="False" />
                    <Condition Binding="{Binding BestMode}" Value="Hybrid" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
            </MultiDataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TestResultsRow" TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
        <Setter Property="MinHeight" Value="56" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsInstalledLocally}" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.PurpleBg}" />
            </DataTrigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Accent}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
    </Style>

    <Style x:Key="ModelsLaunchRow" TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
        <Style.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsBeingTested}" Value="True" />
                    <Condition Binding="{Binding TestingFlashPhase}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Warning}" />
                <Setter Property="Foreground" Value="Black" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding IsBeingTested}" Value="True" />
                    <Condition Binding="{Binding TestingFlashPhase}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
            </MultiDataTrigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Accent}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="CatalogInstalledRow" TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
        <Style.Triggers>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding Installed}" Value="True" />
                    <Condition Binding="{Binding RefreshState}" Value="None" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RefreshState}" Value="Complete" />
                    <Condition Binding="{Binding RefreshHighlight}" Value="Installed" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RefreshState}" Value="Complete" />
                    <Condition Binding="{Binding RefreshHighlight}" Value="Description" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.InfoBg}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RefreshState}" Value="Processing" />
                    <Condition Binding="{Binding RefreshFlashPhase}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Warning}" />
                <Setter Property="Foreground" Value="Black" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RefreshState}" Value="Processing" />
                    <Condition Binding="{Binding RefreshFlashPhase}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RefreshState}" Value="Testing" />
                    <Condition Binding="{Binding RefreshFlashPhase}" Value="True" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Warning}" />
                <Setter Property="Foreground" Value="Black" />
            </MultiDataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding RefreshState}" Value="Testing" />
                    <Condition Binding="{Binding RefreshFlashPhase}" Value="False" />
                </MultiDataTrigger.Conditions>
                <Setter Property="Background" Value="{StaticResource Brush.Bg}" />
                <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
            </MultiDataTrigger>
            <DataTrigger Binding="{Binding RefreshHighlight}" Value="TestedUndownload">
                <Setter Property="Background" Value="{StaticResource Brush.InfoBg}" />
            </DataTrigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.Accent}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="ToolkitFlashButtonTemplate" TargetType="Button">
        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="ToolkitButtonTemplate" TargetType="Button">
        <Border x:Name="Border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Bg}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Bg}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ToolkitButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Brush.Button}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Margin" Value="0,0,8,0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template" Value="{StaticResource ToolkitButtonTemplate}" />
    </Style>

    <Style x:Key="CatalogDescriptionModeActive" TargetType="Button" BasedOn="{StaticResource ToolkitButton}">
        <Setter Property="Background" Value="{StaticResource Brush.ActiveBg}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Active}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Active}" />
    </Style>

    <Style x:Key="ModeCardButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource Brush.Button}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="MinHeight" Value="104" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Bg}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource Brush.Bg}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PlaceholderText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.Muted}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Margin" Value="16" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="ToolkitProgressBar" TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}" />
        <Setter Property="Foreground" Value="{StaticResource Brush.Accent}" />
        <Setter Property="BorderBrush" Value="{StaticResource Brush.PanelBorder}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <ControlTemplate x:Key="ToolkitAiFeatureCheckBoxTemplate" TargetType="CheckBox">
        <Grid Background="Transparent" MinHeight="48">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border x:Name="Box"
                    Grid.Column="0"
                    Width="22" Height="22"
                    Margin="0,0,10,0"
                    VerticalAlignment="Center"
                    Background="{StaticResource Brush.Panel}"
                    BorderBrush="{StaticResource Brush.PanelBorder}"
                    BorderThickness="2"
                    CornerRadius="2" />
            <Path x:Name="CheckMark"
                  Grid.Column="0"
                  Width="14" Height="11"
                  Margin="4,0,16,0"
                  VerticalAlignment="Center"
                  Stretch="Uniform"
                  Stroke="#FF2222"
                  StrokeThickness="3"
                  Data="M 1 5 L 5 9 L 13 1"
                  Visibility="Collapsed" />
            <ContentPresenter Grid.Column="1"
                              VerticalAlignment="Top"
                              RecognizesAccessKey="True" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                <Setter TargetName="Box" Property="BorderBrush" Value="#FF2222" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource Brush.Warning}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ToolkitAiFeatureCheckBox" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Padding" Value="4,6" />
        <Setter Property="Margin" Value="0,6,0,6" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template" Value="{StaticResource ToolkitAiFeatureCheckBoxTemplate}" />
    </Style>
</ResourceDictionary>